// Allow tagging of GLSL shaders to enable syntax highlighting
export function frag (strings) {
  return strings.join()
}

export function vert (strings) {
  return strings.join()
}

export function glsl (strings) {
  return strings.join()
}
